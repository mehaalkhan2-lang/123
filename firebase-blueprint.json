{
  "entities": {
    "GalleryImage": {
      "title": "Gallery Image",
      "description": "An image uploaded to the achievements gallery.",
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "The URL of the image."
        },
        "alt": {
          "type": "string",
          "description": "Alternative text for the image."
        },
        "uploadedBy": {
          "type": "string",
          "description": "The email of the user who uploaded the image."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The time the image was uploaded."
        }
      },
      "required": ["url", "alt", "uploadedBy", "createdAt"]
    },
    "Broadcast": {
      "title": "Broadcast",
      "description": "A notification broadcast sent by the admin.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "body": { "type": "string" },
        "sentAt": { "type": "string", "format": "date-time" },
        "sender": { "type": "string" }
      },
      "required": ["title", "body", "sentAt", "sender"]
    },
    "PushToken": {
      "title": "Push Token",
      "description": "An FCM push token for a user.",
      "type": "object",
      "properties": {
        "token": { "type": "string" },
        "uid": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["token", "uid", "updatedAt"]
    },
    "Visitor": {
      "title": "Visitor",
      "description": "A tracked visitor of the website with their device information.",
      "type": "object",
      "properties": {
        "userAgent": { "type": "string" },
        "device": { "type": "string" },
        "os": { "type": "string" },
        "browser": { "type": "string" },
        "language": { "type": "string" },
        "screenResolution": { "type": "string" },
        "firstVisit": { "type": "string", "format": "date-time" },
        "lastActive": { "type": "string", "format": "date-time" }
      },
      "required": ["userAgent", "device", "os", "browser", "language", "screenResolution", "firstVisit", "lastActive"]
    }
  },
  "firestore": {
    "achievements_gallery/{imageId}": {
      "schema": "GalleryImage",
      "description": "Collection of gallery images for the achievements section."
    },
    "broadcasts/{broadcastId}": {
      "schema": "Broadcast",
      "description": "Admmin broadcasts sent to all victims."
    },
    "push_tokens/{tokenId}": {
      "schema": "PushToken",
      "description": "Storage for FCM push tokens."
    },
    "visitors/{visitorId}": {
      "schema": "Visitor",
      "description": "Collection of tracked website visitors and their devices."
    }
  }
}
